# 实验契约

未来任何性能实验都必须固定并记录以下内容：

- Git commit
- GPU 型号和驱动版本
- CUDA、PyTorch、vLLM 版本
- 模型名称和量化格式
- 输入 Token 分布
- 输出 Token 分布
- 请求率和最大并发
- warmup 时间
- 正式运行时长
- SLO 阈值
- vLLM 启动参数
- 控制器配置
- 原始结果路径

禁止选择性删除失败结果。若实验失败，应记录失败原因、错误日志路径和是否重试。

没有满足上述契约的记录时，不得在 README 或简历描述中填入性能提升数字。
